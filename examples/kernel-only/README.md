# Files-only witness (synthetic)

No Task, Skill, Router or Runtime is used. Copy this directory into a new disposable directory, then begin at [project overview](project-overview.md). All decisions and facts here are illustrative, not a real user's evidence.

1. Fresh reader: recover stable identity `example-notebook`, the adopted limit of 12, its predecessor 8, and the unadopted proposal 20.
2. In your disposable copy, authorize a limit change to 16 for this exercise. Preserve the old 12 fact and its provenance under history; update current and stage with the new decision/date and a history link. Do not adopt proposal 20 implicitly.
3. Close the conversation. A fresh reader starts from the same overview and recovers 16 as current, 12/8 as history, and 20 as pending. No workflow engine or old chat is needed.

The repository tests perform this update in a temporary copy, validate preservation and reread from disk. That is a deterministic witness, not a human acceptance claim.
